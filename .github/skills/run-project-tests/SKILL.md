---
name: run-project-tests
description: Run the complete Omnigen test suite. Use when asked to test the whole project or verify repository-wide changes.
---

# Run the whole project test suite

From the repository root, run:

```shell
pnpm test
```

This runs the Vitest projects across the monorepo. Report any failing test names and their error output.

