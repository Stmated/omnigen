{
  "name": "@omnigen/duo-openrpc-java-test",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "dev": "tsc -w",
    "lint": "TIMING=1 eslint src/**/*.ts* --fix",
    "test": "jest --passWithNoTests"
  },
  "jest": {
    "preset": "@omnigen/jest-presets/jest/node"
  },
  "dependencies": {
    "@omnigen/core-log": "*",
    "@omnigen/parser-openrpc": "*",
    "@omnigen/target-java": "*"
  },
  "devDependencies": {
    "@omnigen/eslint-config": "*",
    "@omnigen/jest-presets": "*",
    "@omnigen/tsconfig": "*",
    "@omnigen/utils-test": "*",
    "@omnigen/utils-test-target-java": "*",
    "typescript": "4.9.1-beta"
  }
}
