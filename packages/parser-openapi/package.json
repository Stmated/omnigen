{
  "name": "@omnigen/parser-openapi",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "dev": "tsc -w",
    "lint": "TIMING=1 eslint src/**/*.ts* --fix",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@omnigen/api": "workspace:*",
    "@omnigen/core": "workspace:*",
    "@omnigen/core-json": "workspace:*",
    "@omnigen/core-log": "workspace:*",
    "@omnigen/core-plugin": "workspace:*",
    "@omnigen/parser-jsonschema": "workspace:*",
    "json-pointer": "^0.6.2",
    "json-schema": "^0.4.0",
    "openapi-types": "^12.1.3",
    "yaml": "^2.8.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@omnigen/config-typescript": "workspace:*",
    "@omnigen/eslint-config-custom": "workspace:*",
    "@omnigen/utils-test": "workspace:*",
    "@types/json-pointer": "^1.0.34",
    "@types/json-schema": "^7.0.15",
    "tsx": "^4.20.3",
    "typescript": "5.9.2"
  }
}
