{
  "name": "@omnigen/omnigen",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "vite build & tsc -d --declarationDir dist/ --emitDeclarationOnly",
    "generate": "ts-node src/index.ts --pretty $@",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "dev": "tsc -w"
  },
  "dependencies": {
    "@omnigen/core": "*",
    "@omnigen/parser-jsonschema": "*",
    "@omnigen/parser-openrpc": "*",
    "@omnigen/target-java": "*"
  },
  "devDependencies": {
    "@omnigen/config-jest": "*",
    "@omnigen/config-typescript": "*",
    "@omnigen/eslint-config-custom": "*",
    "@types/node": "^16.18.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  }
}
